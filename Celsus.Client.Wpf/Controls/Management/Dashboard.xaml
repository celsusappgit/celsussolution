<UserControl x:Class="Celsus.Client.Wpf.Controls.Management.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Celsus.Client.Wpf.Controls.Management"
             mc:Ignorable="d" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:controls="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls"
             d:DesignHeight="450" d:DesignWidth="800"
             HorizontalAlignment="Center" VerticalAlignment="Top" MinWidth="800" MinHeight="600" >
    <StackPanel>
        <DockPanel Background="{StaticResource  BackLightest}">
            <Border DockPanel.Dock="Top" Style="{DynamicResource BorderCommands}">
                <DockPanel Style="{DynamicResource TopDockPanel}">
                    <TextBlock Text="Sources" Style="{DynamicResource TextBlockTitle01}"/>
                </DockPanel>
            </Border>
            <Border Style="{DynamicResource BorderContent}">
                <telerik:RadTreeView Grid.IsSharedSizeScope="True">
                   
                </telerik:RadTreeView>
            </Border>

        </DockPanel>
    </StackPanel>
</UserControl>
